# 微光 App 项目总纲

> 本文件是项目的长期事实来源，记录产品范围、模块设计、技术路线和实时进度。每次完成重要功能或调整范围时同步更新。

最后更新：2026-09-03

## 1. 产品愿景

“微光”是一款帮助用户制定计划、培养习惯和管理每日待办的轻量应用。产品强调低压力、可持续和清晰反馈：不追求复杂的项目管理，而是让用户每天都知道“下一件值得做的事”。

目标用户：希望改善生活节奏、容易被复杂效率工具劝退、需要温和正反馈的个人用户。

核心原则：

- 简洁：首屏只突出今天最重要的内容。
- 温和：使用鼓励式文案，弱化失败与惩罚感。
- 可见：通过进度、连续天数和周回顾呈现积累。
- 易迁移：浏览器版先验证体验，数据结构为后续 iOS 版本保留清晰边界。

## 2. 当前产品结构

### 今天

用途：承载用户每天最常使用的操作。

- 展示当日日期、问候语、综合完成率。
- 创建、编辑、完成、取消完成、删除待办；时间与备注分开保存，编辑时可修改日期和关联计划。
- 将待办移动到其他日期时，完成状态会随待办一起迁移。
- 展示习惯进度并支持快速累计。
- 所有变更自动保存到当前设备；无需账号，也不会上传计划内容。

实现状态：已完成 MVP。

### 计划

用途：管理需要多天或多步骤完成的目标。

- 创建计划，并可随时编辑名称、分类与截止日期。
- 创建、完成和删除计划里程碑。
- 根据第一个未完成里程碑自动计算整体进度和下一步行动；新增、完成或删除里程碑后即时更新。
- 创建待办时可关联计划，计划详情会汇总关联待办。
- 设置或修改截止日期，并显示剩余天数和逾期状态。
- 将已完成计划归档，历史里程碑和关联待办保持可查，并可随时恢复。
- 可直接删除计划；删除前明确确认，关联待办与完成记录保留并自动解除计划关联。

实现状态：已完成。

### 习惯

用途：记录需要重复完成的行为。

- 创建习惯并设置每日目标次数。
- 编辑名称、图标字、目标单位、执行周期与提醒；可暂停或恢复。
- 删除习惯时同步清理历史记录，并取消 iOS 待处理通知。
- 今日累计与完成状态。
- 连续天数和最近一周完成格。
- 自定义执行星期、提醒时间和暂停状态。
- 休息日不计入完成率，也不会中断连续记录。
- 在“今天”切换历史日期后，可补记或撤销每项习惯的具体次数，周报与月报即时更新。
- iOS 版按执行星期发送系统本地通知。

实现状态：周期、提醒、暂停、历史补记和 iOS 系统通知已完成。

### 回顾

用途：让用户看见一段时间内的行动规律。

- 汇总完成待办、完成习惯和计划平均进度。
- 展示最近七天完成率图表。
- 展示真实月度完成率热力图并与上月比较。
- 提供一条行为洞察文案。

实现状态：真实周报和月报已完成；更丰富的专注时段分析待开发。

### 快速新增

- 从桌面端按钮或移动端中央按钮打开。
- 可切换创建待办、习惯或计划。
- 支持键盘操作，Esc 关闭弹窗。

实现状态：已完成。

### 首次启动

- 全新安装默认从空白数据开始，不混入演示完成记录。
- 首屏说明无账号、本机保存和三个核心模块。
- 用户可主动载入示例内容，现有升级用户不会重复看到引导。

实现状态：已完成。

## 3. 数据设计

当前自用阶段以本机数据为权威来源：浏览器使用 localStorage，iOS 使用 Capacitor Preferences。离线时所有核心功能仍可操作；换设备前需要主动导出 JSON 备份，再在新设备恢复。

- `weiguang.snapshot.v4`：浏览器和 iOS 的本机缓存与离线副本；旧版数据会自动迁移。
- `records[date]`：按日期保存待办完成 ID 和每项习惯的实际累计值。
- 旧版浏览器 D1 接口没有身份边界，现已固定返回 `410 Gone`，防止共用快照继续被读取或覆盖；历史数据库内容未删除。

未来若增加多设备同步，必须先建立账号或端到端加密的设备身份边界，再设计冲突合并；不会恢复无身份的共用快照。

正式版本建议的数据关系：计划可包含多个里程碑，里程碑可关联多个待办；习惯每天生成一条记录；所有实体使用稳定 UUID 和创建/更新时间，为云同步做准备。

## 4. 技术路线

当前技术：React + TypeScript + Vinext，使用响应式 CSS 实现桌面端与手机端共用界面。应用可在浏览器运行，也可通过携带完整页面资源的 Windows 离线包直接使用；离线包不依赖公开站点。

iOS 测试版已采用 Capacitor 8：`mobile/` 提供静态入口，`mobile-dist/` 是随 App 安装的本地资源，`ios/` 保存 Xcode 工程。iOS 版不加载远程网页，断网也可打开；数据通过 Capacitor Preferences 保存在设备，本地通知负责习惯提醒，Haptics 提供完成反馈。应用从后台恢复时会自动处理跨日刷新，并在已有通知授权下重新校准本地提醒。`npm run ios:preflight` 会在所有平台核对发布资源、隐私资料与配置，并在 macOS 上追加无签名模拟器编译。

iOS 版数据备份已使用 Filesystem 写入临时 JSON 文件，再通过系统分享面板保存到“文件”、AirDrop 或其他用户选择的位置；网页版继续使用浏览器下载，两个平台共享同一备份格式。

iOS 演进建议：

1. 浏览器 MVP：验证信息结构、主要交互和视觉风格。
2. PWA：增加离线缓存、安装图标和通知能力。
3. iOS 测试版：优先使用 Capacitor 封装现有前端，接入本地通知、触感反馈和安全存储，快速进入 TestFlight。
4. 原生增强：若后续需要 Apple Watch、小组件、HealthKit 或更深系统体验，再逐步迁移关键页面到 SwiftUI；数据模型与同步 API 保持不变。
5. 云同步：增加用户身份和云端数据库，支持 iPhone、iPad 与网页之间同步。

## 5. 视觉与体验规范

- 风格：浅色毛玻璃、低饱和薰衣草紫、雾蓝和暖杏色。
- 圆角：主要卡片 20–28px，控件 9–16px。
- 动效：克制、快速，并尊重系统“减少动态效果”设置。
- 响应式：大屏三栏，中屏两栏，小屏单栏加底部导航。
- 可访问性：交互元素提供可读标签；保证键盘操作、焦点样式和足够触控尺寸。
- 文案：短句、具体、鼓励式表达，不制造焦虑。

## 6. 里程碑与进度

### M0：项目与体验骨架（已完成）

- [x] 初始化项目与运行环境
- [x] 建立毛玻璃视觉系统
- [x] 完成桌面端与移动端响应式布局
- [x] 建立产品总纲

### M1：可交互浏览器 MVP（已完成）

- [x] 今天、计划、习惯、回顾四模块导航
- [x] 待办新增、完成、取消完成、删除
- [x] 待办编辑、改期与完成状态迁移
- [x] 习惯新增与当日累计
- [x] 习惯完整编辑、安全删除与提醒清理
- [x] 计划新增与进度推进
- [x] 计划名称、分类与截止日期编辑
- [x] 计划安全删除并保留关联待办与完成记录
- [x] 快速新增弹窗与操作反馈
- [x] localStorage 本机持久化
- [x] 基础回顾数据与趋势图
- [x] 公开免登录站点发布与品牌分享预览
- [x] Windows 可安装 PWA 版本与安装说明

### M2：真实日历与历史记录（已完成）

- [x] 按日期保存每日记录
- [x] 支持昨天、今天、未来日期切换
- [x] 计划里程碑与计划关联待办
- [x] 计划截止日期、逾期提示与归档恢复
- [x] 自定义习惯周期、目标、提醒时间与暂停
- [x] 历史日期习惯补记与撤销
- [x] 用真实记录生成周报
- [x] 月度报告
- [x] 数据导入、导出，并将“恢复示例数据”和“清空全部数据”拆分为独立确认操作
- [x] 浏览器与 iOS 本机持久化；停用无身份云同步接口

### M3：PWA 与 iOS 测试版

- [x] 离线运行与安装清单
- [x] 首次访问资源预缓存、版本失效和真实离线刷新
- [x] App 图标和系统主题适配
- [x] iOS 启动页
- [x] Capacitor iOS 工程
- [x] 本地通知与触感反馈（代码与资源已完成，待真机验收）
- [x] 前后台切换后的跨日刷新与提醒校准
- [x] iOS 原生备份导出与系统分享
- [x] 统一 `0.1.0 (1)` 版本并建立 iOS 发布前自动预检
- [x] 应用内隐私与版本说明、隐私政策及 App Store 发布资料草案
- [x] 无登录首次启动引导与空白初始数据
- [x] 键盘焦点可见、弹窗内焦点循环与无障碍操作标签
- [x] Windows 独立窗口运行与本机离线资源服务
- [x] 不依赖 `chatgpt.site` 的 Windows 便携包、品牌启动器与 SHA-256 校验文件
- [x] Windows 单文件安装程序、当前用户安装、桌面与开始菜单快捷方式及卸载入口
- [x] Windows 本机版隐藏浏览器/PWA 的二次安装入口
- [x] 正式站点公开访问，无需登录即可安装使用
- [x] Windows 一键生成带 SHA-256 的 Mac/iOS 交接包
- [x] App Store 非豁免加密声明与 `arm64` 设备能力预检
- [x] 公开隐私政策页、支持页及 App Store 正式网址
- [x] 首版收敛为 iPhone-only，并完成商店截图内容与尺寸规范
- [x] 核心日期、数据迁移、计划进度与待办改期规则自动回归测试
- [x] iOS Bundle 默认语言与商店首发语言统一为简体中文
- [x] PWA 与 iOS 图标统一为无透明通道的标准 1024 × 1024 资源，并加入自动尺寸校验
- [ ] TestFlight 内测

Windows 安装说明见 `WINDOWS_SETUP.md`，iOS 交接说明见 `IOS_SETUP.md`。当前开发机为 Windows，已完成单文件 Windows 安装程序，以及可在此环境验证的 iOS 静态构建、原生工程生成、插件同步和资源生成；Xcode 编译、签名、通知真机授权与 TestFlight 上传必须在 Mac 上完成。

### M4：账号与云同步

- [ ] 登录与访客数据迁移
- [ ] 云端数据库及多端同步
- [ ] 冲突处理、备份和隐私设置

## 7. 验收标准

- 首屏在常见桌面和手机尺寸下无横向滚动、遮挡或不可触达操作。
- 新增或修改内容后刷新页面，数据仍然存在。
- 键盘用户可以完成核心新增流程；弹窗内焦点循环且关闭后回到原入口，普通弹窗可用 Esc 关闭，首次引导需明确选择。
- 构建无 TypeScript 或运行时错误。
- 关键操作提供即时视觉反馈；删除待办、习惯、里程碑、清空数据或恢复示例数据前均需确认，不因误触造成不可恢复的数据丢失。

## 8. 进度维护规则

- 新需求先归入对应模块或里程碑，再开始实现。
- 完成功能后勾选对应条目，并更新“最后更新”日期。
- 若产品方向、数据模型或 iOS 路线改变，必须同步修改本文件。
- 开发时以“完成一个可验证的用户闭环”为最小交付单位。
