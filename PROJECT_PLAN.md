# 微光 App 项目总纲

> 本文件是项目的长期事实来源，记录产品范围、模块设计、技术路线和实时进度。每次完成重要功能或调整范围时同步更新。

最后更新：2026-09-01

## 1. 产品愿景

“微光”是一款帮助用户制定计划、培养习惯和管理每日待办的轻量应用。产品强调低压力、可持续和清晰反馈：不追求复杂的项目管理，而是让用户每天都知道“下一件值得做的事”。

目标用户：希望改善生活节奏、容易被复杂效率工具劝退、需要温和正反馈的个人用户。

核心原则：

- 简洁：首屏只突出今天最重要的内容。
- 温和：使用鼓励式文案，弱化失败与惩罚感。
- 可见：通过进度、连续天数和周回顾呈现积累。
- 易迁移：浏览器版先验证体验，数据结构为后续 iOS 版本保留清晰边界。

## 2. 当前产品结构

### 今天

用途：承载用户每天最常使用的操作。

- 展示当日日期、问候语、综合完成率。
- 创建、完成、取消完成、删除待办。
- 展示习惯进度并支持快速累计。
- 所有变更自动保存到当前设备，并同步到私有云端数据库。

实现状态：已完成 MVP。

### 计划

用途：管理需要多天或多步骤完成的目标。

- 创建计划并设置分类。
- 创建、完成和删除计划里程碑。
- 根据里程碑自动计算整体进度和下一步行动。
- 创建待办时可关联计划，计划详情会汇总关联待办。
- 后续增加截止日期和计划归档。

实现状态：里程碑与关联待办已完成；截止日期和归档待开发。

### 习惯

用途：记录需要重复完成的行为。

- 创建习惯并设置每日目标次数。
- 今日累计与完成状态。
- 连续天数和最近一周完成格。
- 自定义执行星期、提醒时间和暂停状态。
- 休息日不计入完成率，也不会中断连续记录。
- 后续接入系统本地通知和补签。

实现状态：周期、提醒设置和暂停已完成；系统通知和补签待开发。

### 回顾

用途：让用户看见一段时间内的行动规律。

- 汇总完成待办、完成习惯和计划平均进度。
- 展示最近七天完成率图表。
- 展示真实月度完成率热力图并与上月比较。
- 提供一条行为洞察文案。

实现状态：真实周报和月报已完成；更丰富的专注时段分析待开发。

### 快速新增

- 从桌面端按钮或移动端中央按钮打开。
- 可切换创建待办、习惯或计划。
- 支持键盘操作，Esc 关闭弹窗。

实现状态：已完成。

## 3. 数据设计

当前阶段以 Sites D1 数据库为产品数据的权威来源，并在浏览器保存一份本机缓存。离线时仍可操作，恢复联网后自动同步。

- `app_state`：数据库中的版本化产品快照，包含待办、习惯、计划和每日记录。
- `weiguang.snapshot.v3`：浏览器中的本机缓存与离线副本；旧版数据会自动迁移。
- `records[date]`：按日期保存待办完成 ID 和每项习惯的实际累计值。
- 浏览器 API：`GET /api/state` 读取私有云端快照，`PUT /api/state` 保存更新。

当前私有单用户阶段使用完整快照同步；进入多用户和多设备并发阶段前，将拆分为独立关系表并增加基于更新时间的冲突合并。

正式版本建议的数据关系：计划可包含多个里程碑，里程碑可关联多个待办；习惯每天生成一条记录；所有实体使用稳定 UUID 和创建/更新时间，为云同步做准备。

## 4. 技术路线

当前技术：React + TypeScript + Vinext，使用响应式 CSS 实现桌面端与手机端共用界面。应用可在浏览器运行，并可通过托管链接直接体验。

iOS 演进建议：

1. 浏览器 MVP：验证信息结构、主要交互和视觉风格。
2. PWA：增加离线缓存、安装图标和通知能力。
3. iOS 测试版：优先使用 Capacitor 封装现有前端，接入本地通知、触感反馈和安全存储，快速进入 TestFlight。
4. 原生增强：若后续需要 Apple Watch、小组件、HealthKit 或更深系统体验，再逐步迁移关键页面到 SwiftUI；数据模型与同步 API 保持不变。
5. 云同步：增加用户身份和云端数据库，支持 iPhone、iPad 与网页之间同步。

## 5. 视觉与体验规范

- 风格：浅色毛玻璃、低饱和薰衣草紫、雾蓝和暖杏色。
- 圆角：主要卡片 20–28px，控件 9–16px。
- 动效：克制、快速，并尊重系统“减少动态效果”设置。
- 响应式：大屏三栏，中屏两栏，小屏单栏加底部导航。
- 可访问性：交互元素提供可读标签；保证键盘操作、焦点样式和足够触控尺寸。
- 文案：短句、具体、鼓励式表达，不制造焦虑。

## 6. 里程碑与进度

### M0：项目与体验骨架（已完成）

- [x] 初始化项目与运行环境
- [x] 建立毛玻璃视觉系统
- [x] 完成桌面端与移动端响应式布局
- [x] 建立产品总纲

### M1：可交互浏览器 MVP（已完成）

- [x] 今天、计划、习惯、回顾四模块导航
- [x] 待办新增、完成、取消完成、删除
- [x] 习惯新增与当日累计
- [x] 计划新增与进度推进
- [x] 快速新增弹窗与操作反馈
- [x] localStorage 本机持久化
- [x] 基础回顾数据与趋势图
- [x] 私有测试站点发布与品牌分享预览

### M2：真实日历与历史记录（已完成）

- [x] 按日期保存每日记录
- [x] 支持昨天、今天、未来日期切换
- [x] 计划里程碑与计划关联待办
- [x] 自定义习惯周期、目标、提醒时间与暂停
- [x] 用真实记录生成周报
- [x] 月度报告
- [x] 数据导入、导出与重置
- [x] D1 私有云端同步与本机离线缓存

### M3：PWA 与 iOS 测试版

- [x] 离线运行与安装清单
- [x] App 图标和系统主题适配
- [ ] iOS 启动页
- [ ] Capacitor iOS 工程
- [ ] 本地通知与触感反馈
- [ ] TestFlight 内测

### M4：账号与云同步

- [ ] 登录与访客数据迁移
- [ ] 云端数据库及多端同步
- [ ] 冲突处理、备份和隐私设置

## 7. 验收标准

- 首屏在常见桌面和手机尺寸下无横向滚动、遮挡或不可触达操作。
- 新增或修改内容后刷新页面，数据仍然存在。
- 键盘用户可以完成核心新增流程，弹窗可用 Esc 关闭。
- 构建无 TypeScript 或运行时错误。
- 关键操作提供即时视觉反馈，不因误触造成不可恢复的数据丢失。

## 8. 进度维护规则

- 新需求先归入对应模块或里程碑，再开始实现。
- 完成功能后勾选对应条目，并更新“最后更新”日期。
- 若产品方向、数据模型或 iOS 路线改变，必须同步修改本文件。
- 开发时以“完成一个可验证的用户闭环”为最小交付单位。
