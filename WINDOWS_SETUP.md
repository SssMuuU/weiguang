# 微光 Windows 可用版

Windows 版本提供单文件安装程序，核心功能离线运行，也不需要管理员权限。运行 `outputs/微光安装程序-0.2.0.exe` 后，可以选择安装位置；默认安装到 `%LOCALAPPDATA%\Programs\微光`。程序会创建桌面和开始菜单快捷方式。已有旧版时关闭微光并选择原安装目录覆盖安装一次，无需卸载；此后支持应用内更新。

安装版携带完整页面资源，启动后仅在本机 `127.0.0.1` 提供内部页面，不向局域网或互联网开放。界面由 Windows WebView2 Runtime 嵌入“微光”自己的原生窗口，不会启动 Edge 浏览器窗口；任务栏、窗口标题和图标均显示为微光。

程序声明为 Per-Monitor V2 DPI 感知，在 125%、150%、200% 等 Windows 显示缩放下由 WebView2 按屏幕实际像素渲染，避免系统先低分辨率绘制再放大造成文字和边缘模糊。

`outputs/weiguang-windows-0.2.0.zip` 保留为备用便携版；必须完整解压后运行，不能只复制其中的 exe。

## 数据与升级

- 数据只保存在 `%LOCALAPPDATA%\Weiguang\BrowserData` 的微光专用配置中；删除该目录会清空内容。
- 建议定期从“数据与安装”导出 JSON 备份，换电脑或换浏览器后再恢复。
- 启动后后台检查新版，窗口底部提供“检查更新 / 下载更新”；下载期间可继续使用，下载完成后再次确认才会更新并重启。
- 更新前要求关闭编辑窗口并确认数据已保存。固定本机地址和数据目录不变，不做自动数据迁移或清空。
- 下载仅接受微光官网 HTTPS 地址，禁止重定向，检查文件大小、SHA-256 和版本；损坏、超时、断网不会替换旧版。
- 替换前完整解包与启动自检，只替换应用管理的文件；替换或自检失败时恢复旧文件。若恢复遇到文件锁等异常，会保留备份并提示位置，不擅自删除。
- “恢复示例数据”和“清空全部数据”是两个独立操作，都会在执行前确认。

## 当前边界

- Windows 版没有 iOS 本地通知和触感反馈；核心计划、习惯、待办、回顾、备份与离线功能可用。
- Windows 安装程序使用系统 WebView2 Runtime。绝大多数 Windows 10/11 已预装；若系统确实缺少，可通过 Windows Update 补齐。由于没有商业代码签名证书，首次运行时 Windows 可能显示来源提醒。
- 目前更新失败恢复覆盖可捕获的文件操作与自检错误，不保证突然断电或强制终止时的全自动恢复；极端情况下可用安装程序覆盖修复，个人数据独立存放。
- iOS 开发暂时搁置，保留现有工程。

iOS 和浏览器版本继续使用 [微光在线版](https://weiguang-plan-habits.workspace-192140.chatgpt.site)。

## 后续 Windows 发布

1. 增加 `windows/WindowsRelease.cs` 的三段式版本号，更新 `windows/RELEASE_NOTES.txt`。
2. 运行 `npm run windows:package`，自动执行更新安全测试、构建离线界面、启动器/安装器自检，生成安装包和校验文件。
3. 脚本同步生成 `public/windows/latest.json` 和带内容哈希的安装包；确认两者一起进入网站生产构建，再通过 Sites 发布。
4. 发布后检查线上清单和安装包的大小与 SHA-256。新版只有大于当前版本才会提示；不能仅发布网页就声称 Windows 已更新。

`public/windows` 不会复制进入离线应用，避免安装包套安装包。更新功能不上传个人内容，但更新服务会接收常规网络请求信息（如 IP 和应用版本）。目前没有独立发布签名密钥，更新来源安全依赖官网 HTTPS；SHA-256 用于验证完整性，不等同于代码签名。
