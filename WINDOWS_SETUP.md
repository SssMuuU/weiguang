# 微光 Windows 可用版

Windows 首个可用版本采用 PWA：安装后会出现在开始菜单和任务栏中，以独立窗口运行，并继续使用本机存储。它与当前网页版本功能一致，不需要额外账号体系；计划、习惯、待办和记录不会上传到微光服务器。

## 安装

1. 使用 Microsoft Edge 或 Google Chrome 打开微光正式地址。
2. 首次进入后选择“从空白开始”或“先看看示例”。
3. 点击地址栏右侧的“安装微光”图标；若未显示，在浏览器菜单中选择“应用 / 安装微光”。
4. 安装完成后，可从 Windows 开始菜单打开，并固定到任务栏。

若浏览器暂未显示安装入口，先完整打开一次页面，再刷新。也可点右上角“微”进入“数据与安装”，在浏览器提供安装事件时点击“安装应用”。

## 数据与升级

- 数据只保存在当前 Windows 浏览器配置中；清理站点数据或卸载时选择删除浏览数据会清空内容。
- 建议定期从“数据与安装”导出 JSON 备份，换电脑或换浏览器后再恢复。
- 新版本发布后会自动更新缓存；关闭并重新打开微光即可使用新版本。
- 首次完整打开后，断网仍可查看和修改数据；重新联网后无需同步。

## 当前边界

- Windows PWA 没有 iOS 本地通知和触感反馈；核心计划、习惯、待办、回顾、备份与离线功能可用。
- iPhone 可从 Safari 使用“分享 → 添加到主屏幕”安装同一网页版本；原生 iOS 测试版仍需在 Mac 上完成签名与 TestFlight 验收。
