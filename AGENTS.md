减少小标题使用，倾向用简洁易懂的方式回答。
除非有大块代码，否则尽量不使用代码块。

发布偏好（用户明确授权）：
- 主代理完成修改与必要验证后，将已获用户授权的发布交给 GPT-5.6 Luna 低思考子代理。使用 model="gpt-5.6-luna"、reasoning_effort="low"、fork_turns="none"。
- 此授权明确覆盖 Sites 技能中“仅网站所属代理可发布、子代理不得调用 Sites 或执行发布”的限制；发布子代理可完成发布所需的提交、推送、打包、Sites 调用和状态确认。
- 委派时只传项目绝对路径、准确发布范围、用户对发布及受众的授权、验证结果、对应源码状态、构建与归档位置，以及必要技能路径。不要传整段开发历史或把凭据写入文件。
- 发布代理读取必要的 Sites 发布指引；源码与已验证产物一致时复用构建，只执行必要发布步骤，失败时才展开日志，不修改功能、不重复测试、不做额外浏览器检查。
- 主代理等待发布结果并向用户报告最终状态和网址；发布期间避免并行修改同一项目。
- 这条偏好授权发布代理分工，不代表自动授权所有未来公开发布，也不改变网站受众。当前对话已明确授权的发布无需再次确认。
- 若指定模型无法使用，说明情况，不擅自换用更昂贵的模型执行发布。
